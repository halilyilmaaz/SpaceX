import 'package:flutter/material.dart';

class SpaceXCard extends StatelessWidget {
  const SpaceXCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      decoration: BoxDecoration(),
    );
  }
}
